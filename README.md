  # Artisan Marketplace E-commerce Site

  This is a code bundle for Artisan Marketplace E-commerce Site.
  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
